<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestBlankForm.MainPage">
    <StackLayout Margin="10,35,10,10">
        <Label Text="CIS 260 Notes"
               HorizontalOptions="Center"
               FontAttributes="Bold" />
        <Editor x:Name="editor"
                Placeholder="Enter your CIS 260 note"
                HeightRequest="100" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Text="Save"
                    Clicked="OnSaveButtonClicked" />
            <Button Grid.Column="1"
                    Text="Delete"
                    Clicked="OnDeleteButtonClicked"/>
        </Grid>
        
        <!-- new testing stuff ~Josh-->

        <Label HorizontalOptions="Center" VerticalOptions="Center" Text="Hello World!" x:Name="SliderLabel"></Label>
        
        <Button HorizontalOptions="Center" VerticalOptions="Center"
                Clicked="Button_Clicked"></Button>
        <Slider ValueChanged="Slider_ValueChanged" x:Name="Slider"/>

    </StackLayout>
</ContentPage>